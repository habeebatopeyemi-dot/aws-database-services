SQL QUERIES

I opened pgAdmin and clicked on the server at the left side, then navigated to the table to select query tools.

I created the table and executed successfully

I created two INSERTS and also executed successfully

I also performed the READ, UPDATE and DELETE operations successfully and the result is in the screenshot uploaded

DYNAMODB SETUP

I logged into my AWS console and searched for DynamoDB, then created a table, give the table name, name the partition key and set it to string and create.
